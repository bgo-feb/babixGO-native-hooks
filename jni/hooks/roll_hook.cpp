#include "../hook_manager.h"

namespace Hooks::Roll {

bool Install() {
    // Placeholder only. Feature hooks stay out of the MWE until the loader path
    // and the diagnostic hook are stable on-device.
    return false;
}

}  // namespace Hooks::Roll

